server:
  port: 6616

spring:
  application:
    name: carrentals-service

message: Car Rental Service Configuration Loaded

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
